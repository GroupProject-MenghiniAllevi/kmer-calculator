
class DSKInfo:

    def getFullKmerNumber(self):
        """
        Questo metodo serve per calcolare il numero totale di k-mer presenti in tutti i file presi in input.
        :return: il numero totale di k-mer presenti.
        """

    def getSingleKmerNumber(self, filepath):
        """
        Questo metodo serve per calcolare il numero di k-mer presenti in un file preso in input.
        :param filepath: il file di input.
        :return: il numero di k-mer presenti
        """


    def iteration_number(self, file_disk_space):
        """
        Questo metodo serve per calcolare il numero totale di iterazione che l'algoritmo deve eseguire.
        :param file_disk_space: la dimensione totale dello spazio che questo programma deve occupare nel disco,
        espressa in bit
        :return:
        """

    def get_partition_number(self,memory_usage):
        """
        Questo metodo restituisce il numero totale di partizioni che l'algoritmo deve creare per salvare
        i k-mer nel disco.
        :param memory_usage la memoria utilizzata dall'algoritmo, in bit.
        :return: numero totale di partizioni
        """

